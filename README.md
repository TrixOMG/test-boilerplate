# Привет!

Это твое тестовое задание. Мы подготовили для тебя стартовый проект (Boilerplate). Твоя задача — развернуть его локально и дописать недостающий функционал

# Важно:

- Надо сделать fork данного проекта, затем создать отдельную ветку в которой будет вестись вся последующая работа

- Каждую задачу нужно выполнять отдельным коммитом, должно получиться 6 коммитов

- После завершения работ со всеми задачами надо сделать PR (Pull Request) в главную ветку

# Задачи:

1. Проект сейчас не запускается. В нем есть ошибки. Твой первый шаг — запустить базы данных через Docker и исправить все ошибки, чтобы приложение успешно стартовало. Также кратко описать в README.md, какие ошибки мешали проекту запуститься изначально

- **Решение**:

    - дописал docker-compose.yml, добавил Dockerfile для запуска NestJS

    - при билде обнаружил ошибку:

        ```bash
            4.196 src/modules/comments/comments.service.ts:5:54 - error TS2304: Cannot find name 'CommentsRepository'.
            4.196
            4.196 5     constructor(private readonly commentsRepository: CommentsRepository) {}
        ```

        создал _src/modules/comments/comments.repository.ts_
        импортировал в _src/modules/comments/comments.service.ts_
        добавил репозиторий в провайдеры _src/modules/comments/comments.module.ts_

    - также были ошибки с моделями, их решил добавлением imports в модулях _src/modules/comments/comments.module.ts_ и _src/modules/posts/posts.module.ts_ **эти изменения сделал в коммите задачи 3**

---

2. Подключить ConfigModule и настроить валидацию env переменных через JOI

3. Дописать CRUD для Постов (PostgreSQL). Также надо создать DTO схемы с валидацией (class-validator)

4. Дописать CRUD для Комментариев (MongoDB). Также надо создать DTO схемы с валидацией (class-validator)

5. Настроить дополнительную валидацию: Комментарий с оценкой от 2 до 4 баллов (включительно) обязательно должен содержать текст отзыва (не менее 10 символов). Если оценка 1 или 5 — текст может быть пустым

6. Добавить Swagger на все эндпоинты и DTO

Далее надо в другом репозитории написать небольшой frontend (желательно React, но можно использовать и другой фреймворк), с возможностью использовать весь функционал данного NestJS приложения. По завершению работ надо будет предоставить ссылку на репозиторий с frontend и ссылку на PR

